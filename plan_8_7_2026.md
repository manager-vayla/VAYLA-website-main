# VAYLA SEO + GEO + Public Facts Remediation Plan

Ngày bắt đầu: 2026-08-07  
Phạm vi: `vayla.io`, mã nguồn trong repository hiện tại, các trang public và dữ liệu chính thức của VAYLA.  
Nguyên tắc: chỉ công khai claim có nguồn; dữ liệu động phải có nguồn và thời điểm cập nhật; nếu nguồn không có dữ liệu thì hiển thị `Unavailable`.

## Baseline audit

- [x] Kiểm kê SEO hiện có, routes, hard-coded metrics và external links.
- [x] Quét runtime route chính bằng `dev-browser` ở local static export và production; kiểm tra interaction đầy đủ của mọi control vẫn là hạng mục riêng cần mở rộng.
- [x] Đối chiếu whitepaper chính thức `(EN)_VAYLA_WHITEPAPER_v3.8.pdf` từ nhánh `stitch_design`.
- [x] Đối chiếu link-tree chính thức và các nguồn public CMC/CoinGecko/BscScan.

### Findings P0/P1

- [x] `public/llms.txt` đã được tạo trong source và local export; production vẫn cần publish vì hiện `/llms.txt` trả 404.
- [x] Nội dung `/whitepaper` đã thay bằng summary/link whitepaper v3.8, bỏ version/claim/contract không khớp.
- [x] UI và OG image active đã bỏ giá, TVL, holders, volume, vaults và claim `fan-funded equity` không có nguồn.
- [x] `/arena`, `/chart`, `/tokenutility`, `/doc` đã có route React tương ứng; local export có route HTML riêng.
- [x] Metadata, canonical và external link registry đã được chuẩn hóa theo route/nguồn chính thức.
- [x] Đã ghi nhận CMC `untracked`, market data không khả dụng và CoinGecko coin id `vayla`; UI dùng `Unavailable` thay vì suy đoán.

## Workflow và dependency check

- [x] Xác nhận workflow chính thức là [`Deploy Vayla Website ver 3.0`](https://github.com/manager-vayla/VAYLA-website-main/actions/workflows/deploy.yaml), file `.github/workflows/deploy.yaml`; không có workflow/action `pages build and deployment`, `actions/deploy-pages`, `configure-pages` hoặc `upload-pages-artifact`.
- [x] Xác nhận workflow `Deploy Vayla Website ver 3.0` dùng root `package.json` + root `pnpm-lock.yaml`, `pnpm install --frozen-lockfile`, Node 24 và Next static export; không cài/chạy dependency của app Vite legacy bên trong `vayla-deliverable/vayla-react`.
- [x] `pnpm install --frozen-lockfile --offline` pass; root typecheck/lint/build không conflict với workflow dependency tree.
- [x] Workflow có bước kiểm tra `out/llms.txt`, asset SEO và các route `index.html` trước FTP upload; `trailingSlash: true` khớp cấu trúc static export.
- [ ] Đọc lại kết quả một lần deploy production sau khi workflow chạy; hiện chưa có quyền xác nhận FTP/server state trong turn này.

## Phase 1 — Source of truth, official links và llms.txt

- [x] Tạo module facts tập trung: network, token supply, contract, features, token allocation, source URLs, `lastUpdated`.
- [x] Tạo module official links tập trung, bao gồm website, Arena, link-tree, whitepaper v3.8, GitHub, BscScan, CMC, CoinGecko, X, Telegram và email.
- [x] Thêm team developer vào dữ liệu public: CTO Pham Thanh Binh, Senior FE Van Minh Hoang, Senior BE Nguyen Van Thanh, kèm LinkedIn/GitHub do user cung cấp.
- [x] Tạo `public/llms.txt` với nội dung ngắn, có cấu trúc và không chứa dữ liệu chưa xác minh.
- [x] Đồng bộ các footer/header links với official links registry.

## Phase 2 — Whitepaper, token, claims và hình ảnh

- [x] Thay nội dung whitepaper in-app bằng summary đúng v3.8 và link trực tiếp PDF chính thức.
- [x] Xóa claims giả lập: Ethereum/Base/Arbitrum, ERC-4626, địa chỉ `0xVay1a...`, audit chưa xác minh, equity/yield/guaranteed return.
- [x] Đồng bộ token facts và allocation với whitepaper v3.8: 3B supply, BNB Smart Chain/BEP-20, 45/15/15/15/10.
- [x] Sửa các claim trong Token, Start, Roadmap, Arena, Mechanism và Legal để dùng ngôn ngữ utility/participation đúng nguồn.
- [x] Sửa OG image, manifest và alt text; bỏ URL/claim cũ `vayla.xyz`, `fan-funded equity`, số liệu mô phỏng.
- [x] Xóa dead wallet/staking path (`ConnectButton`, `wagmi`, `DepositModal`, fake `useApp` balance/positions) và dependency `zustand`; public site chỉ còn preview không cần ví.

## Phase 3 — Metrics và technical SEO/GEO

- [x] Tạo data adapter với trạng thái `verified`, `unavailable`, `illustrative`, source và timestamp.
- [x] Thay metrics hard-code bằng adapter; không hiển thị số mô phỏng như live data.
- [x] Chuẩn hóa CoinGecko id/link; không gọi dữ liệu market nếu API trả null/untracked.
- [x] Bổ sung route thật hoặc redirect rõ ràng cho `/arena`, `/chart`, `/tokenutility`, `/doc`.
- [x] Tạo metadata riêng theo route: title, description, canonical, OG/Twitter, H1 và JSON-LD phù hợp.
- [x] Cập nhật Organization/Person/WebSite JSON-LD và loại schema FAQ không có nội dung visible tương ứng.
- [x] Cập nhật sitemap chỉ gồm URL canonical/indexable routes có nội dung; local export và local HTTP trả 200. Production directory-index behavior vẫn cần kiểm tra sau deploy.
- [x] Giữ robots.txt cho phép crawler và thêm `llms.txt` vào quy trình publish/documentation.

## Phase 4 — Full UI checklist và verification

- [x] Inventory source-level global navigation, cookie consent, legacy wallet modal path và external links; dead wallet modal đã được xóa.
- [x] Inventory source-level homepage, `/vaults`, `/vault/:slug`, `/dashboard`, `/ai`, `/marketplace`.
- [x] Inventory source-level `/whitepaper`, `/creator`, `/onboarding`, `/start`, `/legal/*`, `/token`, `/calculator`.
- [ ] Kiểm tra runtime mọi route fallback, button, input, select, accordion, modal, loading/error/disabled state (source inventory đã xong; route smoke bằng `dev-browser` đã pass local, interaction coverage đầy đủ còn pending).
- [x] Chạy build, lint, typecheck, static export, local HTTP route smoke, metadata/H1, sitemap/robots, route smoke bằng `dev-browser` và `git diff --check`.
- [x] Visual QA bằng Chrome local ở desktop/mobile cho homepage, creator preview, dashboard và whitepaper; đã ghi nhận các lỗi responsive cần sửa ở backlog.
- [x] Quét claim P0/P1 trong active render scope bằng `rg`; các claim còn lại trong component legacy không được import vào `DeliverableApp`.

## Acceptance criteria

- [x] `/llms.txt` có facts/links có nguồn và trả HTTP 200 trong local export/local HTTP; production cần xác nhận sau deploy (baseline production từng trả 404).
- [x] Whitepaper page khớp v3.8 và không còn version/claim/contract giả lập trong source/export.
- [x] Mọi metric public có source + timestamp hoặc hiển thị `Unavailable`.
- [x] Mọi indexable route local có title, description, canonical và 1 H1 riêng; legal subroutes có metadata riêng và alias `/brutal-math` được `noindex`.
- [x] Sitemap local chỉ chứa canonical routes có nội dung; không chứa route fallback/alias/directory index. Production cần readback sau deploy.
- [ ] Các nút và trường UI chính hoạt động đúng trên desktop/mobile.
- [x] Build/lint/static smoke test và route smoke bằng `dev-browser` hoàn tất; `git diff --check` pass.

## Phase 5 — UI/UX and anti-AI-slop refinement

- [x] Rà soát visual QA homepage, creator preview, dashboard và whitepaper ở desktop/mobile từ artifacts Chrome hiện có.
- [x] Sửa navbar mobile: không để quick menu, CTA dài và hamburger cạnh tranh cùng lúc; giữ rõ đây là public dashboard preview, không phải đăng nhập.
- [x] Sửa nguyên nhân overflow bằng `min-width: 0`, width theo viewport và wrapping có chủ đích cho hero, creator stepper/form, whitepaper và cookie consent.
- [x] Đổi cookie copy sang privacy/public-preview trung tính; loại bỏ claim analytics/vault tracking không phù hợp.
- [x] Bỏ numeric meta-label kiểu `01 / PARTICIPATE`, `02 / CHART` khỏi active homepage và đổi sang label chức năng thực tế; whitepaper chapter labels cũng được làm tự nhiên hơn.
- [x] Bổ sung focus-visible ring, reduced-motion toàn cục và giảm viền gradient trang trí trên thesis card.
- [x] Final source/export regression: `git diff --check`, stale-label scan, route HTML existence và workflow conflict scan đều pass; `out/` có asset SEO cần publish.
- [ ] Hoàn tất browser interaction test cho mọi control; route/status/SEO smoke đã chạy bằng `dev-browser`, nhưng chưa có coverage tự động cho từng modal/input/accordion state.
- [ ] Chụp lại toàn bộ route bằng một browser session ổn định sau khi môi trường browser/WSL khả dụng.

### Icon, separator and AI-slop cleanup

- [x] Removed decorative marquee stars, CTA icon/arrow, menu chevrons and external-link arrow glyphs from the active React UI.
- [x] Replaced token card numbers and decorative dot separators with semantic labels and plain text separators.
- [x] Replaced visible middle-dot separators, ellipsis placeholders and legal-link arrows in active routes with readable text.
- [x] Changed roadmap controls to text buttons (`Previous` / `Next`) and FAQ disclosure indicators to `Open` / `Close`.
- [x] Re-scanned active components, routes and styles: no visible em dash, arrow, star, decorative icon or ellipsis remains; remaining matches are comments only.
- [x] Added root and nested `.gitignore` rules for browser QA output, build/cache folders, TypeScript incremental files, test reports, logs and OS metadata without ignoring source/assets.

## Change log

- 2026-08-07: Hoàn tất UI/UX refinement vòng 1: mobile overflow fixes, public-preview cookie copy, semantic labels, creator stepper, whitepaper wrapping, focus/reduced-motion và ESLint ignore cho browser QA artifacts.
- 2026-08-07: Final recheck sau mobile-break correction: typecheck pass, lint 0 errors/18 non-blocking warnings, static build 24/24 routes pass.

- 2026-08-07: Tạo kế hoạch từ audit repo/live UI, whitepaper v3.8 và public market sources.
- 2026-08-07: Hoàn tất source-of-truth, `llms.txt`, whitepaper/token claim remediation, route SEO và inventory UI controls. Production audit còn thấy `/llms.txt` 404 và non-root directory index; dependency tree app con thiếu package nên chưa thể hoàn tất build/browser smoke test.
- 2026-08-07: Hoàn tất metadata QA: manifest/JSON-LD hợp lệ, bỏ pháp nhân/FinanceApplication/SearchAction/giá 0/AI claim chưa có nguồn; thêm `trailingSlash: true` cho static export để lần publish tiếp theo tạo route `index.html` đúng cấu trúc.
- 2026-08-07: Hoàn tất workflow/dependency audit: chỉ giữ FTP deploy workflow, root pnpm lockfile pass, không có GitHub Pages publisher; bổ sung export guards cho `llms.txt` và route index files.
- 2026-08-07: Chuẩn hóa tên workflow trong plan thành `Deploy Vayla Website ver 3.0` theo workflow chính thức trên GitHub.
- 2026-08-07: Hoàn tất public-preview remediation cho dashboard/creator/legal/footer, metadata riêng cho legal subroutes, sitemap indexable routes và local HTTP/metadata smoke.
- 2026-08-07: Xóa dead staking store/modal và `zustand`; final source/export scan không còn wallet integration hoặc fabricated staking UI trong active/relevant scope.

## Evidence / blockers

- Icon audit evidence: active nested React components/routes contain no visible em dash, Unicode arrow, star, decorative external-link glyph or ellipsis after the final source scan. Remaining matches are comments or non-rendered legacy CSS text.
- Validation: root `pnpm exec tsc --noEmit`, `pnpm run lint` (0 errors), `pnpm run build` (24/24 static routes) and `git diff --check` pass.
- Nested Vite app standalone build remains unavailable because its local `node_modules` is incomplete (`typescript` and `react-router-dom` cannot be resolved). This app is not used by `.github/workflows/deploy.yaml`; the workflow builds the root Next app, whose checks pass.
- Production HTTP recheck: `/`, `/arena`, `/token`, `/tokenutility`, `/whitepaper`, `/doc`, `/llms.txt`, `/robots.txt` and `/sitemap.xml` all returned HTTP 200; canonical route redirects include trailing slashes where expected.
- Browser visual/interaction recheck remains blocked because the in-app browser disconnected and no browser backend was available for reconnection in this turn.

- Local export: `out/llms.txt` tồn tại; các route indexable có title, description, canonical và 1 H1; `/brutal-math` là alias `noindex`, legal subroutes có metadata riêng.
- Production check ngày 2026-08-07: `/` trả 200; `/arena`, `/chart`, `/tokenutility`, `/doc` và các route non-root trả directory-index HTML; `/llms.txt` trả 404; `robots.txt` và `sitemap.xml` trả 200.
- Official-link HEAD check: website, Arena, link hub, whitepaper PDF, GitHub, CMC, X, Telegram và GitHub developer profiles trả 200; BscScan/CoinGecko trả 403 do anti-bot; LinkedIn trả 405/999 theo phương thức HEAD, không kết luận link hỏng.
- Root workflow dependency check: `pnpm install --frozen-lockfile --offline`, `pnpm exec tsc --noEmit`, `pnpm run lint` và `pnpm run build` đều pass; nested Vite app không thuộc workflow publish hiện tại và không có lockfile riêng được workflow sử dụng.
- `dev-browser` hiện chạy được trên máy và đã dùng để kiểm tra local static export + production; interaction coverage theo từng control vẫn là phần chưa đánh dấu hoàn tất.
- Metadata QA: `public/site.webmanifest` và JSON-LD parse hợp lệ; local HTTP trả 200 cho `/`, `/llms.txt`, `/robots.txt`, `/sitemap.xml` và route chính; `git diff --check` không phát hiện whitespace error.
- Lint pass với 0 errors và 18 existing optimization/style warnings (font loading, `<img>`, hook dependency, anonymous config export); không có warning chặn build.
- Visual QA: desktop homepage/dashboard có hierarchy, contrast và CTA rõ; mobile 390px bị horizontal clipping ở hero headline/deck, navbar CTA + hamburger, creator stepper/form, whitepaper metadata/headline/deck và cookie consent actions.
- UX/content observations: icon người cạnh `Dashboard Preview` dễ bị hiểu là account/login dù chỉ là Quick menu; cookie copy còn cụm `cookie · settlement` và mô tả vault analytics không khớp hoàn toàn với public-preview positioning.
- Browser artifacts: các screenshot QA desktop/mobile được lưu trong thư mục output bị ignore; không thêm công cụ browser test vào dependency repo.

## Phase 6 — dev-browser automatic verification

- [x] Cài `dev-browser` global và tải Chromium bằng `dev-browser install`; đã đọc và áp dụng hướng dẫn skill `C:\Users\DA\.codex\skills\dev-browser\SKILL.md`.
- [x] Tạo checker chính thức [`scripts/check-vayla-dev-browser.js`](scripts/check-vayla-dev-browser.js) và lệnh `pnpm run check:production`; checker rà route desktop/mobile, HTTP status, title, H1, canonical, overflow, glyph AI-slop, wallet/login copy, nhãn button/link, page error, request quan trọng, `llms.txt`, `robots.txt`, `sitemap.xml` và screenshot.
- [x] Thêm bước kiểm tra tự động sau FTP vào workflow [`Deploy Vayla Website ver 3.0`](.github/workflows/deploy.yaml): cài `dev-browser` ở runner, tải Chromium và chạy `pnpm run check:production`; không thêm package vào dependency tree hoặc lockfile của repo.
- [x] Kiểm tra local static export bằng `dev-browser`: 22 lượt route/viewport trả HTTP 200, có title/H1/canonical, không có `pageerror`; 404 còn lại chỉ là request prefetch tĩnh/analytics bị `serve` từ chối và đã được phân loại thành warning.
- [x] Sửa source trước khi deploy: clock trong `GlobalChrome` render deterministic để tránh hydration mismatch; `useVaultsData` không gọi endpoint `/api/vaults` không tồn tại trong public static deployment; thêm route và metadata `/legal/refunds`.
- [x] Recheck production bằng `dev-browser` sau cấu hình: `/`, các route chính, `/llms.txt`, `/robots.txt`, `/sitemap.xml` trả 200; checker vẫn bắt đúng lỗi của bản đang live gồm React error `#418` trên các route và `/legal/refunds` HTTP 404.
- [ ] Chạy lại workflow sau khi deploy commit source hiện tại; chỉ đánh dấu production pass khi checker không còn React `#418` và `/legal/refunds/` trả 200. FTP/server state chưa được thay đổi trực tiếp trong turn này.
