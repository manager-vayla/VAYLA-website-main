name: 🚀 Deploy Vayla Website Stitch Version

on:
  push:
    branches:
      - feat/stitch-design

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # Nếu dùng pnpm thì không để cache: 'npm' ở đây

      - name: 📦 Install pnpm
        run: npm install -g pnpm

      - name: 📦 Install dependencies
        run: pnpm install # Dùng pnpm thay vì npm

      - name: 🔨 Build Project
        run: pnpm run build # Dùng pnpm thay vì npm
        env:
          NEXT_PUBLIC_BASE_URL: https://vayla.io

      - name: 📝 Create .htaccess for Routing
        # Bước này cực kỳ quan trọng để các trang như /chart, /arena không bị lỗi 404
        run: |
          echo "RewriteEngine On
          RewriteBase /
          RewriteCond %{REQUEST_FILENAME} !-f
          RewriteCond %{REQUEST_FILENAME} !-d
          RewriteRule ^([^/]+)/?$ \$1.html [L]" > ./out/.htaccess
      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./out/
          # CHỈNH SỬA TẠI ĐÂY:
          # Vì tài khoản FTP của bạn trỏ vào /home/ptbinh, ta phải đẩy vào public_html
          server-dir: ./public_html/
          # Nếu bạn muốn xóa các file cũ không còn tồn tại trong repo:
          delete: false
